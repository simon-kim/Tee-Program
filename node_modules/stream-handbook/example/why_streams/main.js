var shoe = require('shoe');
var through = require('through');

shoe('/sock').pipe(through(function (msg) {
    var div = document.createElement('div');
    div.textContent = JSON.stringify(msg);
    document.body.appendChild(div);
}));
